# Verification List

Each JSON file in this directory represents a verified submission, ready to be synced by the web app.

File name example: `pi_result_username.json`

This folder is processed by `website/js/supabase.js` sync algorithm (`syncVerificationListFromGitHub`).